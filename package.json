{
  "name": "quiz",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm i --prefix client && npm i --prefix server && npm run build --prefix client && npm start --prefix server"
  },
  "author": "",
  "license": "ISC"
}
